# ╔══════════════════════════════════════════════════════════════════╗
# ║                    EducAI — Environment Variables               ║
# ║  Copy this file to .env and fill in the required values         ║
# ╚══════════════════════════════════════════════════════════════════╝

# ─── Docker / Compose ─────────────────────────────────────────────
POSTGRES_USER=educai
POSTGRES_PASSWORD=                         # ⚠️  REQUIRED — your PostgreSQL password
POSTGRES_DB=educai

# ─── Server (Express API) — server/.env ──────────────────────────
PORT=8000
NODE_ENV=development                       # development | production
LOG_LEVEL=info

# Database
DATABASE_URL_LOCAL=postgresql://educai:YOUR_PASSWORD@localhost:5432/educai   # ⚠️  REQUIRED
DATABASE_URL_CLOUD=                        # ⚠️  REQUIRED in production

# Security — Arcjet
ARCJET_KEY=                                # ⚠️  REQUIRED — https://arcjet.com

# JWT
JWT_SECRET=                                # ⚠️  REQUIRED — generate with: openssl rand -base64 32
JWT_EXPIRES_IN=15m
REFRESH_JWT_SECRET=                        # ⚠️  REQUIRED — generate with: openssl rand -base64 32
REFRESH_JWT_EXPIRES_IN=15d

# Google OAuth
GOOGLE_CLIENT_ID=                          # ⚠️  REQUIRED — Google Cloud Console
GOOGLE_CLIENT_SECRET=                      # ⚠️  REQUIRED — Google Cloud Console
GOOGLE_CALLBACK_URL=http://localhost:8000/auth/google/callback

# Frontend URL (for CORS & redirects)
FRONTEND_URL=http://localhost:3000

# Email (Resend)
EMAIL_HOST=smtp.resend.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=resend
EMAIL_PASS=                                # ⚠️  REQUIRED — Resend API key (re_xxx) from https://resend.com
EMAIL_FROM="EducAI <noreply@educai.is-a.dev>"

# ─── Web (Next.js) — web/.env ───────────────────────────────────
SESSION_SECRET_KEY=                        # ⚠️  REQUIRED — generate with: openssl rand -base64 32
BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# ─── AI Server (FastAPI) — ai-server/.env ────────────────────────
API_KEY=                                   # ⚠️  REQUIRED — generate with: openssl rand -base64 32
